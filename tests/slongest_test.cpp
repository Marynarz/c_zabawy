#include <gtest/gtest.h>
#include "../sem/longest.c"

TEST(LeetCodeSubsests, EmptyVectorTest)
{
    ASSERT_TRUE(1);
}

int main(int argc, char **argv)
{
    testing::InitGoogleTest(&argc, argv);

    return RUN_ALL_TESTS();
}